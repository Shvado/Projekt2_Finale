@model IEnumerable<Projekt.Models.Narudzba>

@{
    ViewBag.Title = "Index";

}
@Styles.Render("~/Content/css")

<br />
<br />
<br />

<p>
    @Html.ActionLink("Kreiraj novu", "Create")
</p>
<table class="table">

    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ime_hrana)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ime_pice)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {

        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ime_hrana)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ime_pice)
            </td>
            <td>
                @Html.ActionLink("Ispravi", "Edit", new { id = item.Id_narudzba }) |
                @Html.ActionLink("Detalji", "Details", new { id = item.Id_narudzba }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id_narudzba })
            </td>
        </tr>
    }


</table>
